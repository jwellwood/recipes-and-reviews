import { GetRatingPipe } from './get-rating.pipe';

describe('GetRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new GetRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
